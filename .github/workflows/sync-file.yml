name: Sync Remote File Hourly with Notifications

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  sync-file:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.PAT_TOKEN }}

    - name: Setup Git
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"

    - name: Download remote file
      id: download
      run: |
        # 尝试下载文件，最多重试3次
        for i in {1..3}; do
          if curl -s -f -o interface.txt https://raw.githubusercontent.com/develop202/migu_video/refs/heads/main/interface.txt; then
            echo "✅ 文件下载成功 (尝试 $i/3)"
            echo "DOWNLOAD_SUCCESS=true" >> $GITHUB_ENV
            break
          else
            echo "❌ 文件下载失败 (尝试 $i/3)"
            sleep 5
          fi
        done
        
        if [ "$DOWNLOAD_SUCCESS" != "true" ]; then
          echo "❌ 下载失败，达到最大重试次数"
          exit 1
        fi
        
        # 检查文件是否有变化
        if git diff --quiet interface.txt 2>/dev/null || [ ! -f interface.txt ]; then
          echo "📝 文件没有变化"
          echo "NO_CHANGES=true" >> $GITHUB_ENV
        else
          echo "📤 文件有变化"
          echo "NO_CHANGES=false" >> $GITHUB_ENV
          
          # 获取变化的行数
          CHANGES=$(git diff --stat interface.txt | tail -1)
          echo "CHANGES_STAT='$CHANGES'" >> $GITHUB_ENV
        fi

    - name: Commit and push if changed
      if: env.NO_CHANGES == 'false' && env.DOWNLOAD_SUCCESS == 'true'
      run: |
        git add interface.txt
        git commit -m "🔄 每小时同步更新 [$(date +'%H:%M')]
        
        变化统计: ${{ env.CHANGES_STAT }}"
        git push origin main
      env:
        GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}

    - name: Create status summary
      if: always()
      run: |
        STATUS=$([ "${{ job.status }}" = "success" ] && echo "✅ 成功" || echo "❌ 失败")
        CHANGES_STATUS=$([ "${{ env.NO_CHANGES }}" = "false" ] && echo "有更新" || echo "无变化")
        TIME=$(date '+%Y-%m-%d %H:%M:%S')
        
        cat > sync-status.md << EOF
        ## 同步状态报告
        - **状态**: $STATUS
        - **时间**: $TIME
        - **文件变化**: $CHANGES_STATUS
        - **下次同步**: 1小时后
        EOF

    - name: Upload status artifact
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: sync-status
        path: sync-status.md
